# core/constants.py

# 模块说明：全局常量定义
# 描述：
# 本模块集中定义了项目运行所需的各种固定值，包括项目信息、日志系统配置、
# 网络请求设置以及业务逻辑中使用的优先级数值等。
"""
模块说明：全局常量定义
描述：
集中管理项目中的配置常量、日志系统、网络请求头及业务逻辑优先级。
"""


# ======================================================================
# 项目元数据
# 用于记录项目的基本信息，例如作者和代码仓库地址。
# ======================================================================

# 项目作者的名称
PROJECT_AUTHOR = "Azleo"
# 项目的 GitHub 仓库地址
PROJECT_REPO = "https://github.com/Azleo/ProxyAssetsHub"


# ======================================================================
# 日志等级系统
# 定义不同类型的日志消息和控制日志输出详细程度的等级。
# ======================================================================

# 消息类型常量定义：

# 错误级别：表示程序遇到了严重问题，无法继续执行。
MSG_ERROR = "ERROR"
# 警告级别：表示程序遇到了非致命性问题，可以继续运行，但可能存在风险。
MSG_WARN = "WARN"
# 信息级别：表示正常的程序流程反馈，如任务启动或完成。
MSG_INFO = "INFO"
# 调试级别：包含程序底层运行的详细技术细节，通常用于开发和排查问题。
MSG_DEBUG = "DEBUG"

# 日志输出等级控制 (Level)：

# Level 0：默认等级。只显示最关键的日志，如程序启动、停止、错误和最终总结。
LOG_LEVEL_DEFAULT = 0
# Level 1：信息等级。显示详细的执行进度，例如“正在处理文件 X”等流程信息。
LOG_LEVEL_INFO = 1
# Level 2：调试等级。显示所有底层技术细节，通常会包含时间戳等额外信息。
LOG_LEVEL_DEBUG = 2

# 日志输出风格控制 (Style)：

# Human 风格：日志格式美观，适合人类阅读，默认情况下会隐藏时间戳以保持简洁。
LOG_STYLE_HUMAN = "human"
# CI 风格：日志格式极简，适合机器解析（例如在持续集成/部署环境，如 GitHub Actions 中使用）。
LOG_STYLE_CI = "ci"


# ======================================================================
# 网络请求配置
# 定义进行网络下载时使用的默认设置。
# ======================================================================

# 默认的 HTTP 请求头（Headers）
DEFAULT_REQUEST_HEADERS = {
    # 'User-Agent' 是一个关键的请求头，用于告诉服务器发送请求的客户端是什么。
    # 这里模拟了一个主流浏览器的身份，以防止被服务器识别为爬虫程序而拒绝提供服务。
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
}


# ======================================================================
# 规则类型优先级配置
# 用于决定在最终输出文件中，不同类型的规则应该以什么顺序排列。
# 数值越小，表示该规则类型的优先级越高，在文件中的位置越靠前。
# ======================================================================

# 规则类型与对应优先级的字典
RULE_TYPE_PRIORITY = {
    # --- 域名匹配类规则 ---
    # DOMAIN 和 HOST：最高优先级，精确匹配单个域名。
    "DOMAIN": 1,
    "HOST": 1,
    # DOMAIN-SUFFIX 和 HOST-SUFFIX：次高优先级，匹配特定后缀的所有域名（如：*.google.com）。
    "DOMAIN-SUFFIX": 2,
    "HOST-SUFFIX": 2,
    # DOMAIN-WILDCARD 和 HOST-WILDCARD：通配符匹配。
    "DOMAIN-WILDCARD": 3,
    "HOST-WILDCARD": 3,
    # DOMAIN-KEYWORD 和 HOST-KEYWORD：包含特定关键词的域名。
    "DOMAIN-KEYWORD": 4,
    "HOST-KEYWORD": 4,
    # --- User-Agent 匹配规则 ---
    # 根据发起请求的应用程序或浏览器类型进行匹配。
    "PROCESS-NAME": 5,
    "USER-AGENT": 5,
    # --- IP 地址类规则 ---
    # IP-CIDR：基于 IPv4 地址段进行匹配。
    "IP-CIDR": 6,
    # IP-CIDR6 和 IP6-CIDR：基于 IPv6 地址段进行匹配。
    "IP-CIDR6": 7,
    "IP6-CIDR": 7,
    # GEOIP：根据 IP 地址的地理位置信息（国家/地区）进行匹配。
    "GEOIP": 8,
    # IP-ASN：根据自治系统号（ASN）进行匹配。
    "IP-ASN": 9,
}

# 默认优先级：用于那些未在 RULE_TYPE_PRIORITY 字典中明确定义的未知或特殊规则类型。
# 赋予一个较大的值，通常意味着它们会被排在最后。
DEFAULT_PRIORITY = 999
